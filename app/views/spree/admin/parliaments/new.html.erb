<% content_for :page_title do %>
  <%= Spree.t :new, scope: :owls %>
<% end %>

<%= form_for [:admin, @parliament], :html => { :multipart => true } do |f| %>
  <%= render partial: 'form', :locals => { :f => f } %>
  <div class="form-actions" data-hook="buttons">
    <%= button Spree.t('actions.create'), 'ok', 'submit', {class: 'btn-success'} %>
    <span class="or"><%= Spree.t(:or) %></span>
    <%= button_link_to Spree.t('actions.cancel'), admin_parliaments_url, :icon => 'remove' %>
  </div>
<% end %>