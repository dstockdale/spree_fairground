<p id="notice"><%= notice %></p>

<h1>Listing Spree Owls</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @spree_slides.each do |spree_slide| %>
      <tr>
        <td><%= link_to 'Show', spree_slide %></td>
        <td><%= link_to 'Edit', edit_spree_slide_path(spree_slide) %></td>
        <td><%= link_to 'Destroy', spree_slide, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Owl', new_spree_slide_path %>
